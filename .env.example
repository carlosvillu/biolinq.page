# Database
# PostgreSQL connection string (works with any PostgreSQL provider)
# Examples:
#   Local Docker: postgresql://postgres:postgres@localhost:5432/biolinq
#   Self-hosted:  postgresql://user:password@your-server.com:5432/biolinq
#   Neon:         postgresql://user:password@ep-xxx.region.aws.neon.tech/biolinq?sslmode=require
# Run `npm run db:up` to start a local PostgreSQL container
DATABASE_URL=

# Better Auth
# Generate secret with: openssl rand -base64 32
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:2025

# Google OAuth (optional)
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Stripe (required for premium payments)
# Get keys from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
# Webhook secret from: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=
# Price ID for the premium product (e.g., price_...)
STRIPE_PRICE_ID=

# Resend (required for contact form emails)
# Get API key from: https://resend.com/api-keys
RESEND_API_KEY=
# Email address that receives contact form messages
CONTACT_EMAIL=
# Sender email address (must use a verified domain in Resend)
# Production: noreply@faviconforge.cc
# Development: onboarding@resend.dev (Resend's test domain)
EMAIL_FROM=

# Google Analytics 4 (optional, for tracking)
# Get Measurement ID from: https://analytics.google.com/
GA_MEASUREMENT_ID=
